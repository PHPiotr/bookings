version: "3"
services:
  web:
    build: .
    ports:
      - "49160:8080"
    links:
      - mongo
    env_file:
      .env
  mongo:
    image: mongo
    volumes:
      - /usr/local/var/mongodb:/data/db
    ports:
      - "27017:27017"